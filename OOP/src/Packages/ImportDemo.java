package Packages;

import java.util.Vector;

public class ImportDemo {

    Vector v = new Vector(); //Accessed directly because of importing at top.

    // Without importing. We are referencing ArrayList using complete package where it needs.

    java.util.ArrayList newlist = new java.util.ArrayList();

    }

